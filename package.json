{
  "name": "nehemiah",
  "version": "0.0.1",
  "description": "A library for managing repositories",
  "keywords": [
    "library",
    "manage",
    "management",
    "managing",
    "mono",
    "repo",
    "repository"
  ],
  "bugs": "https://github.com/n4bb12/ahishar/issues",
  "repository": "github:n4bb12/ahishar",
  "license": "MIT",
  "author": "Abraham Schilling",
  "files": [
    "dist"
  ],
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf dist",
    "compile": "tsc --project tsconfig.json",
    "lint": "tslint --project tsconfig.json --config tslint.json --format stylish --fix",
    "prepublishOnly": "yarn build",
    "test": "echo TODO"
  },
  "devDependencies": {
    "@types/node": "^10.7.1",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  }
}
